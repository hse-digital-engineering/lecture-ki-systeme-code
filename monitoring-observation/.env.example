# Copy this file to .env and adjust as needed

# Camera mode: 'webcam' uses /dev/video0, 'synthetic' generates fake frames
# macOS users: always use synthetic (Docker cannot access Mac webcam easily)
CAMERA_MODE=synthetic
